---
phase: 11-list-picker
plan: 03
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - internal/picker/model.go
  - internal/picker/paramfill.go
  - internal/picker/list_state.go
  - internal/picker/list_state_test.go
  - internal/manage/execute_dialog.go
  - internal/manage/execute_dialog_test.go
autonomous: false
requirements: [LIST-01, LIST-02, LIST-03, LIST-04, LIST-05]

must_haves:
  truths:
    - "User can open a list param and choose from shell-command output in both picker and manage execute"
    - "Selectable rows display the full original row while filtered results renumber visibly"
    - "Selection inserts the extracted final value and confirms that final value before advancing"
    - "Skipped headers never appear as selectable rows"
    - "Empty results, command failures, and parse failures are distinct states with the required recovery behavior"
  artifacts:
    - path: "internal/picker/list_state.go"
      provides: "Dedicated list-picker substate with filtering, numbering, selection, confirmation, and error states"
    - path: "internal/picker/paramfill.go"
      provides: "Picker param fill integration using shared metadata overlay and list state"
    - path: "internal/manage/execute_dialog.go"
      provides: "Manage execute dialog parity for list params"
    - path: "internal/picker/list_state_test.go"
      provides: "Picker list-state tests"
    - path: "internal/manage/execute_dialog_test.go"
      provides: "Manage list-picker tests"
  key_links:
    - from: "internal/picker/paramfill.go"
      to: "internal/params/metadata.go"
      via: "runtime params resolved through shared overlay"
      pattern: "OverlayMetadata"
    - from: "internal/picker/list_state.go"
      to: "internal/params/list_source.go"
      via: "load command output before rendering selectable rows"
      pattern: "LoadListSource"
    - from: "internal/picker/list_state.go"
      to: "internal/params/extract.go"
      via: "post-selection value extraction"
      pattern: "ExtractListValue"
---

<objective>
Implement the runtime list-picker experience in both param-fill entry points using a dedicated filtered substate and the new shared helpers.

Purpose: This is the user-visible Phase 11 behavior: numbered single-select list picking with full-row display, instant filtering, header hiding, extraction confirmation, and strict error handling.

Output: Picker and manage execute dialog list selection flow with automated tests and a final human verification pass.
</objective>

<execution_context>
@/Users/fredriklanga/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/fredriklanga/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-list-picker/11-CONTEXT.md
@.planning/phases/11-list-picker/11-RESEARCH.md
@.planning/phases/04-advanced-parameters-import/04-04-SUMMARY.md
@.planning/phases/09-execute-in-manage/09-01-SUMMARY.md
@internal/picker/paramfill.go
@internal/manage/execute_dialog.go
@internal/picker/search.go

<interfaces>
From internal/picker/paramfill.go:
```go
func initParamFill(m *Model)
func initParamFillCmds(m *Model) []tea.Cmd
func (m Model) updateParamFill(msg tea.KeyMsg) (tea.Model, tea.Cmd)
func (m Model) viewParamFill() string
```

From internal/manage/execute_dialog.go:
```go
type ExecuteDialogModel struct {
	params       []template.Param
	paramInputs  []textinput.Model
	focusedParam int
}

func NewExecuteDialog(wf store.Workflow, width int, theme Theme) ExecuteDialogModel
func (d ExecuteDialogModel) InitCmds() []tea.Cmd
func (d ExecuteDialogModel) Update(msg tea.Msg) (ExecuteDialogModel, tea.Cmd)
```

From internal/picker/search.go:
```go
// Existing in-project filter primitive
// github.com/sahilm/fuzzy is already used for ranked matching
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build dedicated list-picker substate for quick picker param fill</name>
  <files>internal/picker/model.go, internal/picker/paramfill.go, internal/picker/list_state.go, internal/picker/list_state_test.go</files>
  <action>Refactor picker param fill so runtime params come from the shared metadata overlay instead of default-only merging. Add a dedicated list-picker substate in `internal/picker/list_state.go` rather than extending the current inline enum/dynamic renderer. The substate must: load rows from `LoadListSource`; keep full raw rows for display and extraction; render visible rows with trimmed previews only for scannability while preserving the original raw row text identity; hide skipped header rows completely; provide instant filtering as the user types; renumber the currently visible rows after each filter update; allow cursor navigation and numeric direct selection against the visible slice; show a clear empty state when no selectable rows remain after header skipping or filtering; on confirm, call `ExtractListValue` and show the final extracted value that will be inserted; if extraction fails for the chosen row, show an inline parse error and keep the list open so the user can pick again; if the list command fails, show a concise blocking error state with an explicit key to reveal more detail on request and stop the param-fill flow instead of falling back to free text. Keep Bubble Tea v1 and reuse `textinput` plus `sahilm/fuzzy`; do not introduce `bubbles/list` or multi-select.</action>
  <verify>
    <automated>go test ./internal/picker/... -count=1</automated>
  </verify>
  <done>The quick picker can run a list command, show numbered full-row results with instant filtering, confirm the extracted value, retry after parse errors, and abort cleanly on command failures.</done>
</task>

<task type="auto">
  <name>Task 2: Mirror list-picker behavior inside manage execute dialog</name>
  <files>internal/manage/execute_dialog.go, internal/manage/execute_dialog_test.go</files>
  <action>Apply the same list metadata overlay and shared helpers to `ExecuteDialogModel` so manage execute matches the picker experience. Keep the list selection as a dedicated subview/state inside the dialog, not the inline enum renderer. Support the same locked behaviors: full-row display, hidden skipped headers, numbered single selection, instant filtering, trimmed previews, renumbered visible results, confirmation showing the extracted final value, retryable parse errors, clear empty state, and concise command failure with optional detail. Reuse the helper package from 11-01 instead of duplicating command execution or extraction logic. Add focused tests for list command loading, filtered renumbering, selection/extraction, parse-error retry, empty-after-skip, and command-failure handling in the dialog model.</action>
  <verify>
    <automated>go test ./internal/manage/... -count=1 -run 'TestExecuteDialog.*List|TestExecuteDialog.*Parse|TestExecuteDialog.*Error'</automated>
  </verify>
  <done>Manage execute dialog reaches feature parity for list params and all list-specific dialog tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify list picker behavior end-to-end in picker and manage</name>
  <files>N/A</files>
  <action>Claude has automated the shared runtime and tests. Human now verifies the final interaction details that matter most for Phase 11: numbering, filtering feel, confirmation wording, and error/empty-state clarity.</action>
  <what-built>Single-select list picker runtime in both quick picker and manage execute, including full-row display, hidden headers, numbered filtered results, extracted-value confirmation, and strict failure handling.</what-built>
  <how-to-verify>
    1. Create or edit a workflow with a `type: list` arg using a command that emits a header row plus multiple data rows.
    2. Run `go run ./cmd/wf pick`, choose the workflow, and enter the list param.
    3. Confirm skipped header rows never appear, visible rows are numbered, and typing instantly filters and renumbers the visible list.
    4. Select a row by number and confirm the UI shows the final extracted value that will be inserted, not just the raw row.
    5. Use a delimiter/field-index combination that cannot parse one row; confirm a parse error appears and the list stays open so you can choose again.
    6. Use a command that fails; confirm the flow stops with a concise error and an optional way to reveal more detail, with no free-text fallback.
    7. Repeat the same checks in `go run ./cmd/wf manage` → execute a workflow from browse so manage parity is confirmed.
  </how-to-verify>
  <resume-signal>Type "approved" or describe the issue you saw.</resume-signal>
  <verify>
    <automated>go test ./internal/picker/... ./internal/manage/... -count=1</automated>
  </verify>
  <done>User confirms numbered selection, instant filtering, confirmation, empty states, parse retry, and command failure behavior work in both runtime entry points.</done>
</task>

</tasks>

<verification>
- `go test ./internal/picker/... ./internal/manage/... -count=1`
- Manual verification confirms picker and manage share the same list behavior and locked error handling.
</verification>

<success_criteria>
- Users can run a shell-backed list param and pick one row in both picker and manage
- Visible rows always show full original output, with filtered numbering recomputed from the visible slice
- Extracted value confirmation, parse retry, empty state, and command failure handling match the locked decisions
- No multi-select, Bubble Tea v2 migration, or quote-aware CSV parsing appears in the implementation
</success_criteria>

<output>
After completion, create `.planning/phases/11-list-picker/11-03-SUMMARY.md`
</output>
