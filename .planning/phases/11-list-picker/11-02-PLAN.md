---
phase: 11-list-picker
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - internal/manage/param_editor.go
  - internal/manage/form.go
  - internal/manage/param_editor_test.go
  - internal/manage/form_test.go
autonomous: true
requirements: [LIST-01, LIST-03, LIST-04, LIST-05]

must_haves:
  truths:
    - "Author can configure a list parameter entirely from the manage editor"
    - "List params expose command, delimiter, field index, and header skip controls"
    - "List settings persist through save and reload without being confused with enum/dynamic metadata"
    - "Unset extraction settings still mean whole-row insertion at runtime"
  artifacts:
    - path: "internal/manage/param_editor.go"
      provides: "List metadata editing UI with soft staging"
      contains: "list_cmd"
    - path: "internal/manage/form.go"
      provides: "Form round-trip of list metadata through ParamEditorModel.ToArgs()"
    - path: "internal/manage/param_editor_test.go"
      provides: "List editor behavior tests"
    - path: "internal/manage/form_test.go"
      provides: "Save/load tests for list args"
  key_links:
    - from: "internal/manage/param_editor.go"
      to: "internal/store/workflow.go"
      via: "ToArgs emits list metadata fields"
      pattern: "List(FieldIndex|Delimiter|Cmd|SkipHeader)"
    - from: "internal/manage/form.go"
      to: "internal/manage/param_editor.go"
      via: "existing workflow args hydrate list editor state"
      pattern: "NewParamEditor"
---

<objective>
Add list picker authoring to the manage editor so users can define and save list params with the exact controls chosen in discovery.

Purpose: Phase 11 needs author-side configuration before runtime selection matters. This plan exposes the locked configuration knobs in `ParamEditorModel` and makes saved list metadata round-trip cleanly.

Output: Manage editor UI and tests for list command, delimiter, 1-based field index, and header skipping.
</objective>

<execution_context>
@/Users/fredriklanga/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/fredriklanga/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-list-picker/11-CONTEXT.md
@.planning/phases/11-list-picker/11-RESEARCH.md
@.planning/phases/10-parameter-crud-per-field-ai/10-01-SUMMARY.md
@.planning/phases/10-parameter-crud-per-field-ai/10-02-SUMMARY.md
@internal/manage/param_editor.go
@internal/manage/form.go
@internal/manage/param_editor_test.go

<interfaces>
From internal/manage/param_editor.go:
```go
var availableTypes = []string{"text", "enum", "dynamic", "list"}

type paramEntry struct {
	name        string
	paramType   string
	defaultVal  string
	options     []string
	dynamicCmd  string
	nameInput       textinput.Model
	defaultInput    textinput.Model
	optionsInput    textinput.Model
	dynamicCmdInput textinput.Model
}

func (m ParamEditorModel) ToArgs() []store.Arg
```

From internal/store/workflow.go after 11-01:
```go
type Arg struct {
	Name           string
	Type           string
	Default        string
	ListCmd        string
	ListDelimiter  string
	ListFieldIndex int
	ListSkipHeader int
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend ParamEditorModel with list metadata fields</name>
  <files>internal/manage/param_editor.go, internal/manage/param_editor_test.go</files>
  <action>Extend `paramEntry` and the expanded param UI to support list metadata without disturbing the existing soft-staging behavior. Add dedicated inputs for list command, delimiter, field index, and header skip count. When `paramType == "list"`, show all four controls beneath the default field; keep the command as a full shell command string, keep delimiter literal, treat field index as a 1-based author-facing number, and allow `0`/empty to mean whole-row fallback. Keep header skipping as a configurable count of leading rows to hide entirely. Update `visibleSubFields`, focus routing, commit/cancel helpers, `newParamEntry`, rendering, validation warnings, and `ToArgs()` so list metadata survives type switches in-memory and only list-compatible fields are persisted on save. Do not surface multi-select controls, do not reuse enum options for list rows, and do not auto-convert list config into dynamic config.</action>
  <verify>
    <automated>go test ./internal/manage/... -count=1 -run 'TestParamEditor_(List|VisibleSubFields|ToArgs)'</automated>
  </verify>
  <done>Expanded list params show command/delimiter/field index/header skip controls, those values survive soft staging, and `ToArgs()` emits list metadata while stripping incompatible enum/dynamic metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Persist list metadata through the manage form</name>
  <files>internal/manage/form.go, internal/manage/form_test.go, internal/manage/param_editor_test.go</files>
  <action>Wire the new list metadata through `NewFormModel` and `saveWorkflow` using the same `ParamEditorModel` round-trip pattern as other param types. Add or extend tests to cover loading an existing workflow with a list arg, editing and saving a list arg, and verifying the saved workflow contains the exact list metadata values. Include regression coverage for: 1-based field index persistence, delimiter remaining a literal string, header skip count round-tripping, and field index `0` remaining available as the explicit “use full row” state. Keep the Phase 10 rename/type-change behavior intact and do not add any runtime selection UI in this plan.</action>
  <verify>
    <automated>go test ./internal/manage/... -count=1</automated>
  </verify>
  <done>Users can define list params in manage, save them, reopen the workflow, and see command/delimiter/field index/header skip controls preserved exactly.</done>
</task>

</tasks>

<verification>
- `go test ./internal/manage/... -count=1`
- Open an existing workflow in manage and confirm list settings rehydrate into the editor after save/reopen.
</verification>

<success_criteria>
- Manage authoring exposes all locked list-picker knobs: command, delimiter, field index, header skip
- Field index is clearly 1-based to the author, with unset/0 meaning whole-row behavior
- List metadata survives save/reload and stays separate from enum/dynamic metadata
</success_criteria>

<output>
After completion, create `.planning/phases/11-list-picker/11-02-SUMMARY.md`
</output>
