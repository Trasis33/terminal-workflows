---
phase: 11-list-picker
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/store/workflow.go
  - internal/template/parser.go
  - internal/params/metadata.go
  - internal/params/metadata_test.go
  - internal/params/list_source.go
  - internal/params/list_source_test.go
  - internal/params/extract.go
autonomous: true
requirements: [LIST-01, LIST-03, LIST-04, LIST-05]

must_haves:
  truths:
    - "Saved list parameter metadata survives round-trip through YAML and can drive runtime behavior"
    - "Runtime can overlay stored arg metadata by name instead of only copying defaults"
    - "List extraction uses 1-based field indexing and literal delimiter rules"
    - "Header skipping removes leading rows before selection and can yield an explicit empty result"
  artifacts:
    - path: "internal/store/workflow.go"
      provides: "Persisted list picker metadata on store.Arg"
      contains: "ListCmd"
    - path: "internal/template/parser.go"
      provides: "ParamList type and list-aware runtime param contract"
      contains: "ParamList"
    - path: "internal/params/metadata.go"
      provides: "Shared metadata overlay by param name"
      exports: ["OverlayMetadata"]
    - path: "internal/params/list_source.go"
      provides: "Centralized command execution, row loading, header skipping, diagnostics"
      exports: ["LoadListSource"]
    - path: "internal/params/extract.go"
      provides: "Literal delimiter field extraction helper"
      exports: ["ExtractListValue"]
  key_links:
    - from: "internal/params/metadata.go"
      to: "internal/store/workflow.go"
      via: "arg metadata overlay by matching arg name"
      pattern: "arg\.Name == param\.Name"
    - from: "internal/params/list_source.go"
      to: "internal/params/extract.go"
      via: "raw row preserved for post-selection extraction"
      pattern: "Raw"
---

<objective>
Create the shared list-picker foundation: persisted metadata, metadata overlay, and centralized list-source/extraction helpers.

Purpose: Phase 11 breaks unless saved `type: list` metadata reaches runtime. This plan makes stored arg metadata authoritative, keeps Bubble Tea models thin, and centralizes command/parsing behavior before any UI integration.

Output: List-capable arg schema, shared params helpers, and focused tests covering overlay, extraction, header skipping, and command diagnostics.
</objective>

<execution_context>
@/Users/fredriklanga/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/fredriklanga/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-list-picker/11-CONTEXT.md
@.planning/phases/11-list-picker/11-RESEARCH.md
@.planning/phases/04-advanced-parameters-import/04-04-SUMMARY.md

<interfaces>
From internal/store/workflow.go:
```go
type Arg struct {
	Name        string   `yaml:"name"`
	Default     string   `yaml:"default,omitempty"`
	Description string   `yaml:"description,omitempty"`
	Type        string   `yaml:"type,omitempty"`
	Options     []string `yaml:"options,omitempty"`
	DynamicCmd  string   `yaml:"dynamic_cmd,omitempty"`
}
```

From internal/template/parser.go:
```go
type ParamType int

const (
	ParamText ParamType = iota
	ParamEnum
	ParamDynamic
)

type Param struct {
	Name       string
	Type       ParamType
	Default    string
	Options    []string
	DynamicCmd string
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add list metadata contracts and shared helper package</name>
  <files>internal/store/workflow.go, internal/template/parser.go, internal/params/metadata.go, internal/params/list_source.go, internal/params/extract.go</files>
  <action>Extend `store.Arg` with dedicated list metadata fields: `ListCmd`, `ListDelimiter`, `ListFieldIndex`, and `ListSkipHeader` using YAML keys `list_cmd`, `list_delimiter`, `list_field_index`, and `list_skip_header`. Keep author-facing field index 1-based; store `0` as unset so whole-row fallback remains possible. Extend `template.ParamType` with `ParamList` and add matching list fields to `template.Param`, but do NOT add new inline template syntax this phase. Create `internal/params/metadata.go` with a shared `OverlayMetadata` helper that starts from `template.ExtractParams(command)` results, preserves inline defaults as authoritative, and overlays stored arg metadata by matching param name for `Type`, `Options`, `DynamicCmd`, and all new list fields. Create `internal/params/list_source.go` with centralized command execution and row loading helpers using `exec.CommandContext` with timeout, readable short error text plus optional detail, scanner error checking, header skipping before selectable rows, and explicit empty-after-skip result state. Create `internal/params/extract.go` with literal `strings.Split` extraction semantics only: if field index is unset or delimiter is empty, return the full row; otherwise split literally, trim the selected field, and return a retryable parse error when the selected row does not contain that 1-based field. Do not add quote-aware CSV parsing and do not duplicate command execution logic in picker/manage.</action>
  <verify>
    <automated>go build ./... && go test ./internal/params/... ./internal/template/... -count=1</automated>
  </verify>
  <done>`store.Arg` persists list metadata, `template.Param` can represent list params at runtime, and shared helpers exist for metadata overlay, list loading, header skipping, diagnostics, and post-selection extraction.</done>
</task>

<task type="auto">
  <name>Task 2: Add foundation tests for overlay and list parsing rules</name>
  <files>internal/params/metadata_test.go, internal/params/list_source_test.go</files>
  <action>Create focused tests that lock the phase decisions in place. Cover metadata overlay by param name, including the current failure mode from research: saved `type: list` metadata must override template-extracted text params while leaving inline defaults intact. Cover extraction behavior for whole-row fallback, literal custom delimiters, 1-based field indexing, whitespace trimming, and parse errors when the requested field is missing. Cover header skipping removing leading rows entirely, explicit empty-after-skip state, command failure short-message/detail capture, and scanner error handling for long rows so the helper does not silently ignore `scanner.Err()`. Keep tests at package level in `internal/params` and avoid Bubble Tea UI setup here.</action>
  <verify>
    <automated>go test ./internal/params/... ./internal/template/... -count=1</automated>
  </verify>
  <done>Tests fail if runtime reverts to default-only overlay, if delimiter behavior becomes quote-aware, if field indexing becomes 0-based, or if header skipping/command diagnostics regress.</done>
</task>

</tasks>

<verification>
- `go build ./...`
- `go test ./internal/params/... ./internal/template/... -count=1`
- Inspect saved YAML from a list arg fixture and confirm list metadata keys are emitted with the expected names.
</verification>

<success_criteria>
- Saved workflows can persist command/delimiter/field-index/header-skip metadata for list params
- Runtime has a shared metadata overlay path instead of default-only merging
- Shared helpers enforce literal delimiter extraction and 1-based field selection
- Command execution, header skipping, and diagnostics are centralized for picker/manage reuse
</success_criteria>

<output>
After completion, create `.planning/phases/11-list-picker/11-01-SUMMARY.md`
</output>
